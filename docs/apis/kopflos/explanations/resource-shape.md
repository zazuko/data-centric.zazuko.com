# Resource Shape

The central concept of the Kopflos is the `Resource Shape`. It is a SHACL Shape that describes the behavior of a resource. The shape is used to define multiple aspects of the resource.

## Targets
